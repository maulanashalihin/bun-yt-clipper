# YouTube Clipper Configuration

# Server Configuration
PORT=8000
HOST=localhost

# Download Configuration
DOWNLOAD_DIR=downloads

# Force IPv4 - set to true if VPS IPv6 is blocked (common issue)
FORCE_IPV4=true

# Custom User-Agent (default is Chrome on Windows)
# YT_DLP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36...

# Retry configuration when yt-dlp fails
MAX_RETRIES=3
RETRY_DELAY_MS=2000

# Optional: yt-dlp extra arguments
# For VPS issues, try these combinations:
# YT_DLP_EXTRA_ARGS=--extractor-args "youtube:player_client=web"
# YT_DLP_EXTRA_ARGS=--proxy http://user:pass@proxy:port
# YT_DLP_EXTRA_ARGS=--extractor-args "youtube:po_token=YOUR_TOKEN"
