# YouTube Clipper Configuration

# Server Configuration
PORT=8000
HOST=localhost

# Cookies Configuration
# Path to cookies.txt file (relative to project root or absolute path)
# Leave empty or comment out if not using cookies
COOKIES_PATH=cookies.txt

# Download Configuration
# Directory for downloaded files (relative to project root)
DOWNLOAD_DIR=downloads

# Optional: yt-dlp extra arguments (space separated)
# Example: --extractor-args "youtube:player_skip=webpage,configs,js" --no-check-certificate
YT_DLP_EXTRA_ARGS=

# Optional: FFmpeg extra arguments
FFMPEG_EXTRA_ARGS=
