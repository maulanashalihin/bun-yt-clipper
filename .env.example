# YouTube Clipper Configuration

# Server Configuration
PORT=8000
HOST=localhost

# Download Configuration
DOWNLOAD_DIR=downloads

# Force IPv4 (set to true if VPS IPv6 is blocked)
FORCE_IPV4=true

# Custom User-Agent for yt-dlp (default: Chrome 120 on Windows)
YT_DLP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

# Retry configuration
MAX_RETRIES=3
RETRY_DELAY_MS=2000

# Optional: yt-dlp extra arguments (space separated)
# Example: --extractor-args "youtube:player_skip=webpage,configs,js" --no-check-certificate
# For VPS, you might need PO Token: --extractor-args "youtube:po_token=YOUR_TOKEN"
# Or use proxy: --proxy http://user:pass@proxy:port
YT_DLP_EXTRA_ARGS=

# Optional: FFmpeg extra arguments
FFMPEG_EXTRA_ARGS=
